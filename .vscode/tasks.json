{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build current Go file",
            "type": "shell",
            "command": "go build -o ${workspaceFolder}/release/${fileBasenameNoExtension} ${file}",
            "detail": "Build the currently open Go file"
        }
    ]
}