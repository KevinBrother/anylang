#include "math.h"
#include <stdio.h>

int main() {
  int toes = 10;
  int tuple = 10 * 2;
  int pow_value = pow(10, 2);

  printf("toes: %d, tuple: %d, pow_value: %d \n", toes, tuple, pow_value);

  return 0;
}